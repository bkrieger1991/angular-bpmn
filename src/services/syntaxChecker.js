'use strict';
angular.module('angular-bpmn')
/**
 * This service, once started, checks the syntax of the
 * workflow-objects contained in the workspace.
 */
.service('$syntaxChecker', ['$bpmnWorkspace', function($bpmnWorkspace) {

}]);